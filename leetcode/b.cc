#include <bits/stdc++.h>
using namespace std;

int main() {
    // f[i][j] i-size, j last
    // f[i][j][vis]o
    // f[mask][j]
    // j in mask
    // f[mask][j] += f[mask ^ (1<<j)][i] if nums[i] % nums[j] =
    return 0;
}
