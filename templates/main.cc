/**
 *     author: crixdg
 *     created: {{created}}
 **/
#include <bits/stdc++.h>
#include "debug.h"

using namespace std;

// #ifdef LOCAL
// #else
// #define debug(...) ((void)0)
// #endif

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  __test__();

  return 0;
}
